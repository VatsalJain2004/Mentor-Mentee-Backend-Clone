const supabase = require('../../common/database');

const uploadUsers = async(req,res) => {
    //to upload users from excel file.
}

const viewMentorProfile = async(req,res) => {
  //to view mentor profile
}

const viewMenteeProfile = async(req,res) => {
  //to view mentee profile
}

const assignMenteeToMentor =async(req,res)=> {
  // to assgin 30 mentee to each mentor
}


module.exports = { uploadUsers,viewMentorProfile,viewMenteeProfile,assignMenteeToMentor };